name: Renderer Typecheck Debug

on:
  push:
    branches:
      - feat/create-studio-premium-v4

jobs:
  typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci --ignore-scripts
      - run: npx tsc --noEmit --pretty false
