import { prisma } from "@/lib/prisma"; import { SiteHeader } from "@/components/SiteHeader"; import { SiteFooter } from "@/components/SiteFooter"; import { ProductsBrowser } from "./ProductsBrowser";
export const dynamic="force-dynamic";
export default async function ProductsPage({searchParams}:{searchParams:Promise<{q?:string;category?:string}>}){const sp=await searchParams; const rows=await prisma.product.findMany({where:{active:true},orderBy:{createdAt:"asc"}}); const overrides:Record<string,string>={"standard-t-shirt":"/mockups/plain-white-shirt.webp","pullover-hoodie":"/mockups/category-apparel.webp","trucker-cap":"/mockups/category-apparel.webp"}; const products=rows.map(p=>({...p,images:[overrides[p.slug]||(p.images as string[])[0]]})); return <main className="sf"><SiteHeader/><section className="content-hero content-hero-compact"><span>SHOP</span><h1>Products built to customize.</h1><p>Choose a standard product for instant configuration, or use Custom Quote for jobs that need production review.</p></section><ProductsBrowser products={products} initialQuery={sp.q||""} initialCategory={sp.category||""}/><SiteFooter/></main>}